--- a/winpty/tests/test_pty.py
+++ b/winpty/tests/test_pty.py
@@ -2,6 +2,7 @@
 """winpty wrapper tests."""
 
 # Standard library imports
+import re
 import os
 import time
 
@@ -57,6 +58,11 @@ def _pty_factory():
     return _pty_factory
 
 
+def strip_ansi_and_breaks(text):
+    """Remove ANSI escape codes and line breaks that split paths."""
+    return re.sub(r'\x1b\[[0-9;?]*[a-zA-Z]|\r?\n', '', text)
+
+
 # @pytest.fixture(scope='function', params=[
 #     pytest.lazy_fixture('conpty_provider'),
 #     pytest.lazy_fixture('winpty_provider')])
@@ -70,10 +76,11 @@ def test_read(pty_fixture, capsys):
 
     with capsys.disabled():
         start_time = time.time()
-        while loc not in readline:
+        while loc not in strip_ansi_and_breaks(readline):
             if time.time() - start_time > 5:
                 break
             readline += pty.read()
-    assert loc in readline or 'cmd' in readline
+    readline_clean = strip_ansi_and_breaks(readline)
+    assert loc in readline_clean or 'cmd' in readline_clean
     del pty
