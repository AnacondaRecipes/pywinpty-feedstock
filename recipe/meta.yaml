{% set version = "3.0.2" %}
{% set name = "pywinpty" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  - url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
    sha256: 1505cc4cb248af42cb6285a65c9c2086ee9e7e574078ee60933d5d7fa86fb004
    folder: pywinpty-src
    patches:
      - skip-pywinpty-nuget.patch
      - use_local_winpty-rs.patch

  # winpty-rs: vendored in dependency
  - url: https://github.com/andfoy/winpty-rs/archive/refs/tags/v1.0.4.tar.gz
    sha256: d67f96abc22d4b13efb9a88969caf9929a2e8b1b928843b5ab122455e9640e7f
    folder: winpty-rs-src
    patches:
      #- disable_conpty_backend.patch
      - winpty-rs_use_conda_conpty.patch

build:
  number: 1
  skip: true  # [(not win) or py<39]

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - python
    - pip
    - maturin >=1.1,<2.0
    # WinPTY backend library (not ConPTY - ConPTY is built into Windows)
    - winpty 0.4.3
    - conpty
  run:
    - python
    # Required for WinPTY backend fallback
    - {{ pin_compatible('winpty') }}
    - {{ pin_compatible('conpty') }}

test:
  source_files:
    - pywinpty-src/winpty/tests
  imports:
    - winpty
  source_files:
    - winpty/tests
    - runtests.py
  requires:
    - pip
    - pytest
    - flaky
  commands:
    - pip check
    - python -c "from importlib.metadata import version; assert(version('{{ name }}')=='{{ version }}')"
    - pytest -vvv --deselect="pywinpty-src/winpty/tests/test_ptyprocess.py::test_read" pywinpty-src/winpty/tests
    - set CI_RUNNING=1 && python runtests.py --timeout=60  # [win] Tests detect CI and manage CONPTY_CI per backend (ConPTY vs WinPTY)

about:
  home: https://github.com/andfoy/pywinpty
  license: MIT
  license_file:
    - pywinpty-src/LICENSE.txt
    - THIRDPARTY.yml
  license_family: MIT
  summary: Pseudoterminals for Windows in Python
  description: |
    PyWinpty allows creating and communicating with Windows processes that
    receive input and print outputs via console input and output pipes.
    WinPTY backend is supported.
  doc_url: https://github.com/andfoy/pywinpty
  dev_url: https://github.com/andfoy/pywinpty

extra:
  recipe-maintainers:
    - blink1073
    - ccordoba12
    - andfoy
    - ocefpaf
